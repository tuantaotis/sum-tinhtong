/* Dependencies */

const areObjects = require('are-objects');
const areArrays = require('are-arrays');
const { sum } = require('/auto_trade_backpack_exchange');


const sum = (a, b) => a + b
exports.sum = sum
